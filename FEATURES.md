# 功能特性详解

## 产品定位

MCP市场是一个企业级的元数据管理能力平台，将大数据平台的元数据服务能力标准化、产品化，通过可视化的方式让业务方快速构建专属的MCP服务，解决以下痛点：

1. **数据安全**：通过实体范围配置，精确控制每个MCP服务可访问的数据范围
2. **使用体验**：提供快速生成和精细化构建两种模式，满足不同场景需求
3. **运维管理**：全生命周期管理、实时监控、权限控制和成本统计

## 三大核心模块

### 1. 服务市场（Service Market）

**定位**：元数据能力的"超市"，展示所有可用的原子服务

#### 核心功能
- **能力展示**：12+原子能力卡片式呈现
  - 📋 获取表结构 - 查询表的字段、类型、注释等信息
  - 🔗 获取表血缘 - 追溯表的上下游依赖关系
  - 🎯 获取字段血缘 - 字段级别的血缘关系和转换逻辑
  - 📊 获取表统计信息 - 行数、大小、分区等统计数据
  - 📈 获取指标定义 - 业务指标的计算口径和维度
  - 🔍 获取指标血缘 - 指标的计算链路和数据来源
  - 📺 获取看板配置 - 看板的布局和组件配置
  - 💾 获取看板数据 - 实时查询看板数据
  - 👤 获取表负责人 - 查询所有者和管理员信息
  - 📝 获取表访问日志 - 访问记录和审计追踪
  - ✅ 获取数据质量报告 - 完整性、准确性评估
  - 📉 获取指标趋势 - 指标历史趋势数据

- **多维筛选**：
  - 按元数据类型：表/指标/看板
  - 按能力分类：基础查询/血缘分析/数据质量/指标管理/可视化等
  - 关键词搜索：支持名称和描述搜索

- **详情查看**：
  - 能力详细描述和使用场景
  - API调用示例代码
  - 性能指标：累计调用次数、平均响应时间、热度指数
  - 标签体系：快速识别能力特点

#### 使用场景
- 数据开发人员：寻找表结构、血缘等基础能力
- 业务分析师：查找指标定义、趋势分析能力
- 数据治理团队：浏览质量检查、审计追踪能力
- BI工程师：获取看板配置和数据查询能力

---

### 2. MCP工坊（MCP Workshop）

**定位**：MCP服务的"生产车间"，提供两种构建模式

#### 模式一：快速生成 ⚡

适合场景：标准化业务需求，快速上线

**操作流程**：
1. **选择模板**
   - 💻 数据开发助手：表结构 + 血缘 + 统计 + 负责人
   - 🛡️ 数据治理顾问：统计信息 + 访问日志 + 质量报告
   - 📊 指标分析专家：指标定义 + 指标血缘 + 指标趋势
   - 📈 可视化看板管家：看板配置 + 看板数据

2. **配置信息**
   - 服务名称：为MCP服务命名
   - 服务描述：说明用途和使用场景
   - 包含能力：自动关联模板中的原子能力
   - 调用限制：设置频率限制（如1000次/小时）

3. **设置实体范围**
   - 方式1：规则筛选（动态圈选）
     - 数据库：选择目标数据库
     - 表名正则：如 `user_*`, `order_*`
     - 标签：核心表、用户、订单等
     - 热度阈值：0-100分
     - 业务域：营销、财务、用户运营等
     - 实时预览：表格展示匹配结果
   
   - 方式2：专辑筛选（静态集合）
     - 🎯 营销域核心表：45个实体
     - 💰 财务指标集：28个实体
     - 👥 用户行为表：62个实体
     - 📊 运营看板集：18个实体

4. **预览并生成**
   - 预览完整配置信息
   - 确认后一键生成MCP服务
   - 自动跳转到服务管理

**优势**：
- ⏱️ 3分钟内完成MCP创建
- 📋 预设模板覆盖80%常见场景
- 🎯 界面直观，学习成本低

#### 模式二：精细化选择 🎨

适合场景：个性化需求，复杂业务场景

**核心能力**：
1. **可视化画布**（基于ReactFlow）
   - 拖拽式操作：从工具面板拖拽能力节点到画布
   - 节点连接：通过连线表示能力间的依赖关系
   - 自由布局：支持移动、缩放、对齐
   - 小地图：快速导航大型画布

2. **原子能力库**（左侧面板）
   - 按分类折叠展示：基础查询、血缘分析、数据质量等
   - 点击添加：快速添加到画布
   - 能力详情：图标 + 名称，直观识别

3. **节点交互**
   - 点击节点：右侧抽屉展示详细信息
   - 连接节点：表达能力调用顺序或依赖关系
   - 删除节点：清理不需要的能力

4. **配置面板**（右侧）
   - 服务名称和描述
   - 实体范围配置入口
   - 调用频率限制
   - 实时显示已添加节点数量

5. **实体范围配置**
   - 同快速生成模式，支持规则和专辑两种方式
   - 弹窗式交互，不打断画布编辑

**优势**：
- 🎯 精确控制每个能力的组合
- 🔗 可视化表达能力间的关系
- 🛠️ 适合复杂场景的定制化需求
- 💡 画布式交互更符合设计思维

---

### 3. 服务管理（Service Management）

**定位**：MCP服务的"控制中心"，全生命周期运维

#### 核心功能

##### 3.1 统计概览

顶部四个关键指标卡片：
- 📦 **服务总数**：当前部署的MCP服务数量
- 📊 **今日调用**：今日累计调用次数
- 👥 **活跃用户**：近7天使用过服务的用户数
- ⏱️ **平均响应**：所有服务的平均响应时长

##### 3.2 服务列表

功能强大的表格视图：

**列表字段**：
- 服务名称：醒目显示
- 状态标签：
  - 🟢 运行中（active）- 绿色
  - 🟡 测试中（testing）- 橙色
  - ⚪ 已停用（inactive）- 灰色
- 调用次数：累计调用量，支持排序
- 错误率：百分比显示，超过1%红色警告
- 平均响应：毫秒级，支持排序
- 创建时间：服务创建日期
- 创建人：服务所有者

**操作按钮**：
- ✏️ **编辑**：修改服务配置
- 📊 **监控**：查看监控详情（见3.3）
- 🔐 **权限**：配置访问权限（见3.4）
- ❌ **下线**：停用服务（需二次确认）

**筛选功能**：
- 搜索框：支持名称和描述搜索
- 状态筛选：全部/运行中/测试中/已停用
- 表格排序：点击列标题排序

**分页**：
- 默认每页10条
- 支持切换每页数量
- 快速跳转页码
- 显示总记录数

##### 3.3 监控详情

点击"监控"按钮，从右侧滑出抽屉（宽720px）：

**区块1：服务信息**
- 服务名称和描述
- 状态标签

**区块2：关键指标**（3个卡片）
- 📊 累计调用：总调用次数
- ⚠️ 错误率：百分比，颜色标识
- ⏱️ 平均响应：毫秒级性能

**区块3：趋势图表**（基于ECharts）
- 折线图展示最近7天数据
- 三条曲线：
  - 🔵 调用量：左Y轴
  - 🔴 错误数：左Y轴
  - 🟢 响应时间：右Y轴（独立刻度）
- 支持图例切换显示/隐藏
- 鼠标悬停显示具体数值
- 时间横轴：月/日格式

**区块4：调用日志**
- 最近调用记录表格
- 字段：时间、用户、状态、耗时
- 状态标签：成功（绿色）/失败（红色）
- 可扩展为分页查询

**交互特性**：
- 抽屉动画：从右侧滑入
- 数据实时刷新（Demo中为静态）
- 支持导出报表（扩展功能）

##### 3.4 权限管理

点击"权限"按钮，弹出模态框（宽600px）：

**功能组件**：
- 服务名称显示：明确配置对象
- **Transfer穿梭框**：
  - 左侧：可选用户列表
  - 右侧：已授权用户列表
  - 双向移动：添加/移除权限
  - 搜索过滤：快速定位用户
  - 批量操作：支持多选

**权限级别**（可扩展）：
- 只读（read）：查看数据
- 读写（write）：查看和修改
- 管理（admin）：完全控制

**操作流程**：
1. 从左侧选择用户
2. 点击右箭头添加到已授权列表
3. 点击确认按钮保存配置
4. 系统提示"权限配置成功"

**扩展功能**：
- 团队授权：一次授权整个团队
- 角色授权：基于角色的权限管理
- 有效期设置：临时权限自动过期
- 审批流程：需要审批的权限申请

##### 3.5 成本控制（扩展功能）

虽然Demo未完整实现，但设计了以下能力：

- **资源消耗统计**：
  - CPU/内存使用量
  - 存储空间占用
  - 网络流量统计

- **调用配额**：
  - 设置每日/每月调用上限
  - 超出配额后的降级策略

- **费用预警**：
  - 实时计费展示
  - 阈值告警通知
  - 费用趋势预测

---

## 技术实现亮点

### 1. 组件化设计
- 每个页面拆分为独立组件
- CSS Modules实现样式隔离
- 可复用的通用组件（如EntityScopeModal）

### 2. 类型安全
- 完整的TypeScript类型定义
- 接口类型和Mock数据类型一致
- 减少运行时错误

### 3. 数据流管理
- 使用React Hooks管理状态
- 表单使用Ant Design的Form组件
- 为后续引入Zustand预留空间

### 4. 可视化能力
- **ECharts**：监控趋势图表，支持多Y轴、图例交互
- **ReactFlow**：可视化画布，支持拖拽、连线、缩放

### 5. 用户体验优化
- 加载状态：骨架屏和Spin组件
- 操作反馈：message提示、Modal确认
- 动画效果：卡片悬停、抽屉滑入、页面过渡
- 响应式布局：支持不同屏幕尺寸

### 6. 性能优化
- Vite构建：HMR热更新，开发体验流畅
- 按需加载：Ant Design自动Tree Shaking
- 虚拟滚动：大列表性能优化（可扩展）

---

## 使用流程示例

### 场景1：数据开发人员快速创建MCP

**需求**：需要一个能查询营销域核心表的结构、血缘和统计信息的MCP服务

**操作步骤**：
1. 进入"MCP工坊" → "快速生成"
2. 选择"💻 数据开发助手"模板
3. 修改服务名称为"营销数据分析MCP"
4. 点击"配置范围" → 选择"专辑筛选" → 选择"营销域核心表"
5. 点击"预览配置"检查信息
6. 点击"确认生成"
7. 跳转到"服务管理"查看新创建的服务

**耗时**：< 3分钟

---

### 场景2：数据治理团队构建定制化巡检MCP

**需求**：需要一个能巡检核心表的数据质量、访问日志，并生成报告的MCP服务

**操作步骤**：
1. 进入"MCP工坊" → "精细化选择"
2. 从左侧工具面板添加3个能力节点：
   - 获取表统计信息
   - 获取表访问日志
   - 获取数据质量报告
3. 在画布上连接节点表示调用顺序
4. 点击节点查看详细信息，确认能力符合需求
5. 右侧配置面板填写：
   - 服务名称：数据质量巡检MCP
   - 服务描述：定期巡检核心表质量
6. 点击"配置实体范围" → 选择"规则筛选"：
   - 标签：勾选"核心表"
   - 热度阈值：80分
7. 点击"保存并生成"
8. 系统提示"MCP服务构建成功"

**耗时**：5-8分钟

---

### 场景3：服务运维人员监控MCP调用情况

**需求**：查看"营销数据分析MCP"的调用趋势和错误情况

**操作步骤**：
1. 进入"服务管理"
2. 在搜索框输入"营销数据分析"
3. 找到目标服务，点击"监控"按钮
4. 查看右侧抽屉中的监控详情：
   - 累计调用：3250次
   - 错误率：0.5%（正常）
   - 平均响应：85ms
5. 查看7天趋势图，发现12月7日调用量异常高
6. 查看调用日志，定位到具体用户和时间
7. 关闭抽屉，返回列表

**耗时**：< 2分钟

---

## 业务价值

### 1. 提升数据使用效率
- **Before**：每次需要数据，都要找数据开发人员写SQL
- **After**：通过MCP服务，业务方自助查询数据

### 2. 保障数据安全
- **Before**：给予用户库表权限，容易越权访问敏感数据
- **After**：通过实体范围配置，精确控制可访问的数据范围

### 3. 降低重复开发成本
- **Before**：每个业务方都开发一遍相似的查询逻辑
- **After**：原子能力复用，组合成不同的MCP服务

### 4. 统一运维管理
- **Before**：各业务方自己维护脚本和接口，运维成本高
- **After**：统一的服务管理平台，监控、权限、成本一目了然

### 5. 加速AI应用落地
- **Before**：AI模型需要人工整理和提供数据上下文
- **After**：MCP服务标准化输出元数据，AI快速理解数据语义

---

## 未来规划

### Phase 1: 基础能力完善（当前Demo已实现）
- ✅ 服务市场展示
- ✅ 快速生成模式
- ✅ 精细化选择模式
- ✅ 实体范围配置
- ✅ 服务管理和监控

### Phase 2: 后端集成
- 🔲 对接元数据管理系统API
- 🔲 用户认证和SSO集成
- 🔲 实时监控数据推送
- 🔲 调用日志持久化
- 🔲 权限系统RBAC实现

### Phase 3: 高级功能
- 🔲 MCP版本管理和回滚
- 🔲 A/B测试和灰度发布
- 🔲 智能推荐：根据使用习惯推荐MCP模板
- 🔲 数据血缘可视化：图谱展示
- 🔲 成本优化建议：基于调用分析

### Phase 4: 生态建设
- 🔲 MCP市场：公共MCP模板共享
- 🔲 开发者中心：SDK和API文档
- 🔲 插件机制：支持自定义原子能力
- 🔲 社区论坛：用户交流和问题反馈
- 🔲 企业版增强：多租户、审计、合规

---

## 总结

这个Demo展示了一个完整的MCP市场产品的核心功能和用户体验。通过服务市场、MCP工坊、服务管理三大模块，实现了元数据能力的产品化输出和全生命周期管理。

**核心亮点**：
- 🎯 **两种构建模式**：快速生成 + 精细化选择，满足不同场景
- 🔐 **数据安全可控**：实体范围配置，精确权限管理
- 📊 **可视化交互**：ReactFlow画布 + ECharts图表
- 🛠️ **企业级体验**：Ant Design组件 + 专业视觉设计
- 🚀 **易于扩展**：模块化架构，为后端对接预留接口

适合作为大数据平台元数据管理的产品demo，向业务方和管理层展示MCP市场的价值和能力。
