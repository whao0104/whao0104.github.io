# 快速开始指南

## 📦 环境准备

### 系统要求
- Node.js >= 16.0.0
- npm >= 8.0.0 或 yarn >= 1.22.0

### 检查环境
```bash
node -v  # 应显示 v16.x.x 或更高版本
npm -v   # 应显示 8.x.x 或更高版本
```

---

## 🚀 快速启动

### 1. 安装依赖
```bash
cd /Users/skyhwang/CodeBuddy/20251209173628
npm install
```

安装过程约需20秒，将下载347个依赖包。

### 2. 启动开发服务器
```bash
npm run dev
```

启动成功后，终端会显示：
```
VITE v5.4.21  ready in 310 ms
➜  Local:   http://localhost:3000/
```

### 3. 打开浏览器
自动打开浏览器访问 `http://localhost:3000`，即可看到应用界面。

---

## 🎮 功能演示

### 场景1：浏览服务市场（30秒）

**目标**：了解有哪些元数据原子能力可用

**操作步骤**：
1. 应用启动后，默认进入"服务市场"页面
2. 看到12个原子能力卡片，包括：
   - 📋 获取表结构
   - 🔗 获取表血缘
   - 📊 获取表统计信息
   - 📈 获取指标定义
   - 等等...
3. 试试搜索功能：在搜索框输入"血缘"，筛选出血缘相关能力
4. 试试筛选功能：选择类型"指标"，只显示指标类能力
5. 点击任一卡片，弹出详情窗口，查看：
   - 能力详细描述
   - API调用示例
   - 性能统计数据

**预期效果**：
- 卡片悬停有上浮动画
- 搜索和筛选实时生效
- 详情窗口展示完整信息

---

### 场景2：快速生成MCP（3分钟）

**目标**：用预设模板快速创建一个MCP服务

**操作步骤**：

1. **进入工坊**
   - 点击左侧导航"MCP工坊"
   - 默认在"快速生成"标签页

2. **选择模板**
   - 左侧看到4个预设模板
   - 点击"💻 数据开发助手"卡片
   - 观察：卡片有蓝色边框和浅蓝背景（选中状态）

3. **查看右侧配置**
   - 服务名称：自动填充"数据开发助手"
   - 服务描述：自动填充模板描述
   - 包含能力：显示4个蓝色标签
     - 获取表结构
     - 获取表血缘
     - 获取表统计信息
     - 获取表负责人

4. **配置实体范围**
   - 点击"配置范围"按钮
   - 弹出"配置实体范围"对话框
   - **方式A：规则筛选**
     - 数据库：选择"营销数据库"
     - 标签：勾选"核心表"和"用户"
     - 点击确定
   - **方式B：专辑筛选**（推荐新手）
     - 切换到"专辑筛选"标签
     - 点击"🎯 营销域核心表"卡片
     - 观察：卡片有蓝色边框和浅蓝背景
     - 看到"包含 45 个实体"
     - 点击确定
   - 返回主界面，"实体范围"显示"专辑: 营销域核心表 (45个实体)"

5. **预览和生成**
   - 点击"预览配置"按钮
   - 弹出"MCP服务配置预览"对话框
   - 检查所有配置信息：
     - 服务名称：数据开发助手
     - 包含能力：4个蓝色标签
     - 实体范围：专辑: 营销域核心表 (45个实体)
     - 调用限制：1000次/小时
   - 点击"确认生成"按钮
   - 看到绿色提示"MCP服务生成成功！"
   - 表单自动清空，回到初始状态

**预期效果**：
- 每步操作都有即时反馈
- 选中状态明显
- 生成成功有提示

---

### 场景3：精细化构建MCP（5分钟）

**目标**：用可视化画布自定义组合原子能力

**操作步骤**：

1. **切换到精细化模式**
   - 在"MCP工坊"页面
   - 点击顶部"🔧 精细化选择"标签
   - 看到三栏布局：
     - 左侧：原子能力库（按分类折叠）
     - 中间：空白画布
     - 右侧：配置面板

2. **添加能力节点**
   - 展开左侧"基础查询"分类
   - 点击"📋 获取表结构"
   - 观察：画布上出现一个节点，包含图标和名称
   - 提示："已添加「获取表结构」"
   - 继续添加：
     - 点击"🔗 获取表血缘"（在"血缘分析"分类下）
     - 点击"📊 获取表统计信息"（在"数据质量"分类下）
   - 画布上现在有3个节点

3. **连接节点**（可选）
   - 鼠标悬停在第一个节点右侧圆点
   - 按住鼠标左键拖拽到第二个节点
   - 释放，出现一条连线
   - 表示两个能力有调用关系

4. **查看节点详情**
   - 点击任一节点
   - 右侧滑出抽屉，显示：
     - 能力图标（大号）
     - 能力名称
     - 详细描述
     - 元数据：类型、分类、调用次数、响应时间
   - 关闭抽屉

5. **配置MCP服务**
   - 右侧配置面板
   - 填写：
     - 服务名称："我的定制化MCP"
     - 服务描述："用于测试精细化构建"
   - 点击"配置实体范围"按钮
   - 选择"专辑筛选" → "👥 用户行为表"
   - 确定

6. **保存生成**
   - 点击"保存并生成"按钮
   - 提示："MCP服务构建成功！"
   - 画布自动清空
   - 右侧配置面板重置

**预期效果**：
- 节点拖拽流畅
- 连线动画美观
- 节点数量实时显示

**高级玩法**：
- 使用小地图（右下角）导航大型画布
- 使用控制面板（左下角）缩放和适应视图
- 清空画布重新开始

---

### 场景4：管理和监控MCP（2分钟）

**目标**：查看MCP服务列表和监控详情

**操作步骤**：

1. **进入服务管理**
   - 点击左侧导航"服务管理"
   - 看到四个统计卡片：
     - 服务总数：4
     - 今日调用：11,160
     - 活跃用户：156
     - 平均响应：110ms

2. **浏览服务列表**
   - 下方表格显示4个已创建的MCP服务：
     - 营销数据分析MCP（运行中）- 3250次调用
     - 财务报表MCP（运行中）- 1890次调用
     - 数据质量巡检MCP（测试中）- 420次调用
     - 用户画像MCP（运行中）- 5620次调用
   - 观察：
     - 状态标签颜色不同（绿色/橙色）
     - 错误率超过1%会显示红色
     - 每行有4个操作按钮

3. **搜索和筛选**
   - 在搜索框输入"营销"
   - 表格只显示"营销数据分析MCP"
   - 清空搜索框
   - 选择状态"测试中"
   - 表格只显示"数据质量巡检MCP"
   - 恢复"全部状态"

4. **查看监控详情**
   - 找到"营销数据分析MCP"
   - 点击操作列的"📊 监控"按钮
   - 右侧滑出宽度720px的抽屉
   - 看到：
     - 服务名称和描述
     - 3个统计卡片（累计调用、错误率、平均响应）
     - ECharts折线图：显示7天趋势
       - 蓝线：调用量
       - 红线：错误数
       - 绿线：响应时间
     - 调用日志表格：最近3条记录
   - 鼠标悬停在折线图上，看到具体数值
   - 点击图例可显示/隐藏对应曲线

5. **配置权限**
   - 关闭监控抽屉
   - 点击"营销数据分析MCP"的"🔐 权限"按钮
   - 弹出"配置访问权限"对话框
   - 看到Transfer穿梭框：
     - 左侧：可选用户（张三、李四、王五...）
     - 右侧：已授权用户（空）
   - 选中"张三"和"李四"
   - 点击右箭头，移动到右侧
   - 点击"确定"
   - 提示："权限配置成功"

6. **下线服务**（不建议实际操作）
   - 点击某个服务的"❌ 下线"按钮
   - 弹出确认对话框："确定要下线服务「XXX」吗？"
   - 点击"取消"（避免误操作）

**预期效果**：
- 列表数据准确
- 筛选即时生效
- 抽屉动画流畅
- 图表交互友好

---

## 🎨 界面导览

### 整体布局
```
┌──────────────────────────────────────────────┐
│  Logo + 产品名     用户头像                    │  ← 顶部导航栏（64px）
├──────┬───────────────────────────────────────┤
│      │                                       │
│ 侧边 │                                       │
│ 导航 │          主内容区                      │
│      │                                       │
│ 220px│                                       │
│      │                                       │
└──────┴───────────────────────────────────────┘
```

### 侧边导航（可折叠）
- 🛒 服务市场
- 🔧 MCP工坊
- ⚙️ 服务管理

### 主题色
- 主色：科技蓝 #1890FF
- 成功：绿色 #52C41A
- 警告：橙色 #FAAD14
- 错误：红色 #F5222D
- 背景：浅灰 #F5F5F5

---

## 📊 Mock数据说明

为了演示效果，项目使用了以下Mock数据：

### 原子能力（12个）
- 表类：6个（结构、血缘、字段血缘、统计、负责人、日志）
- 指标类：3个（定义、血缘、趋势）
- 看板类：2个（配置、数据）
- 质量类：1个（质量报告）

### MCP模板（4个）
- 数据开发助手
- 数据治理顾问
- 指标分析专家
- 可视化看板管家

### 元数据专辑（4个）
- 营销域核心表（45个实体）
- 财务指标集（28个实体）
- 用户行为表（62个实体）
- 运营看板集（18个实体）

### MCP服务（4个）
- 营销数据分析MCP（运行中，3250次调用）
- 财务报表MCP（运行中，1890次调用）
- 数据质量巡检MCP（测试中，420次调用）
- 用户画像MCP（运行中，5620次调用）

### 监控数据
- 自动生成最近7天的趋势数据
- 包括调用量、错误数、响应时间

---

## ⚡ 常见问题

### Q1: 端口3000被占用怎么办？
**A**: 修改 `vite.config.ts` 中的端口配置：
```typescript
export default defineConfig({
  // ...
  server: {
    port: 3001, // 改为其他端口
    open: true,
  },
})
```

### Q2: 依赖安装失败？
**A**: 尝试以下方法：
1. 删除 `node_modules` 文件夹和 `package-lock.json`
2. 运行 `npm cache clean --force`
3. 重新运行 `npm install`
4. 如果还不行，尝试使用 `yarn` 或 `pnpm`

### Q3: 页面空白或报错？
**A**: 检查浏览器控制台（F12）的错误信息，常见原因：
- 依赖未安装完整：重新运行 `npm install`
- 浏览器不支持：建议使用Chrome/Edge最新版本
- 端口冲突：参考Q1修改端口

### Q4: 如何修改Mock数据？
**A**: 编辑 `src/data/mockData.ts` 文件，修改对应的数据数组。

### Q5: 如何添加新的原子能力？
**A**: 
1. 在 `src/data/mockData.ts` 的 `mockCapabilities` 数组中添加新对象
2. 刷新页面即可在服务市场看到

### Q6: 精细化选择模式中如何删除节点？
**A**: 
- 方式1：选中节点，按 `Delete` 或 `Backspace` 键
- 方式2：点击工具栏的"清空画布"按钮

### Q7: 如何对接真实的后端API？
**A**: 
1. 创建 `src/api/` 目录
2. 编写API调用函数（使用axios）
3. 替换页面组件中的Mock数据调用

示例：
```typescript
// src/api/capabilities.ts
import axios from 'axios'

export const fetchCapabilities = async () => {
  const response = await axios.get('/api/capabilities')
  return response.data
}

// 在组件中使用
import { fetchCapabilities } from '@/api/capabilities'

useEffect(() => {
  fetchCapabilities().then(data => {
    setCapabilities(data)
  })
}, [])
```

### Q8: 如何部署到生产环境？
**A**: 
```bash
# 1. 构建生产版本
npm run build

# 2. 输出在 dist/ 目录

# 3. 部署到静态服务器（如Nginx）
# 将 dist/ 目录的内容复制到服务器

# 4. 或使用预览命令本地测试
npm run preview
```

---

## 🔧 开发技巧

### 1. 使用浏览器开发工具
- 按 `F12` 打开开发者工具
- Console标签：查看日志和错误
- Network标签：查看网络请求（对接API时）
- React DevTools插件：查看组件状态

### 2. 热更新（HMR）
- 修改代码后，页面会自动刷新
- 不需要手动刷新浏览器
- 状态会保留（大部分情况下）

### 3. TypeScript类型检查
```bash
# 运行类型检查
npx tsc --noEmit
```

### 4. 代码格式化
```bash
# 如果配置了Prettier
npm run format
```

### 5. 查看依赖大小
```bash
npm run build
# 构建完成后会显示各chunk的大小
```

---

## 🎯 下一步

完成快速入门后，建议：

1. **深入了解功能**：阅读 `FEATURES.md` 详细了解每个功能的设计思路

2. **尝试修改**：
   - 修改 `src/data/mockData.ts` 添加自己的数据
   - 调整 `src/layouts/MainLayout.module.css` 修改主题色
   - 在 `src/pages/` 下创建新页面

3. **对接后端**：
   - 设计RESTful API接口
   - 使用axios发起请求
   - 替换Mock数据

4. **部署上线**：
   - 构建生产版本
   - 部署到服务器或CDN
   - 配置Nginx反向代理

5. **持续优化**：
   - 添加单元测试
   - 集成CI/CD
   - 监控性能和错误

---

## 📚 参考资源

- [React官方文档](https://react.dev/)
- [TypeScript官方文档](https://www.typescriptlang.org/)
- [Ant Design组件库](https://ant.design/)
- [ECharts图表库](https://echarts.apache.org/)
- [ReactFlow文档](https://reactflow.dev/)
- [Vite构建工具](https://vitejs.dev/)

---

## 💬 反馈和支持

如果在使用过程中遇到问题或有改进建议，欢迎反馈！

祝您使用愉快！🎉
